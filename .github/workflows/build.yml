name: Build
on:
  push:
    branches:
      - develop
      - main
  pull_request:
    types: [ opened, synchronize, reopened ]
jobs:
  build:
    uses: refinedmods/refinedarchitect/.github/workflows/build.yml@v1.6.4
    with:
      mutation-testing: false
    secrets: inherit
